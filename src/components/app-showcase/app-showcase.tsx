import { Component, h } from '@stencil/core';

@Component({
  tag: 'app-showcase',
  styleUrl: 'app-showcase.scss',
  shadow: false,
})
export class AppShowcase {
  render() {
    return (
      <div>
        test
      </div>
    );
  }
}
